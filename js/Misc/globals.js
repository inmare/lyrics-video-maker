const LYRICS_FORMAT = {
  length: null,
  time: null,
  context: "",
};

const SECOND_LEN_PX = 30;
const VIDEO_FRAME = 30;
const PREVIEW_SIZE = [640, 360];

export { LYRICS_FORMAT, SECOND_LEN_PX, VIDEO_FRAME, PREVIEW_SIZE };
